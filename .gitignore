# Dependencies
node_modules

# SvelteKit / Vite (adapter output at repo root — never commit)
/build
.svelte-kit
.output
.vercel
.netlify
.wrangler
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite

# Caches
.cache
.parcel-cache
.turbo
.eslintcache
.prettiercache
.stylelintcache
*.tsbuildinfo

# Test / coverage
coverage
.nyc_output
playwright-report
test-results
blob-report
playwright/.cache

# Load testing (k6 JSON summaries — local only)
load-tests/results

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env & secrets (never commit real keys)
.env
.env.*
!.env.example
!.env.test
*.pem
*.p12
*.pfx
.id_rsa
*.key
.sentryclirc

# SQLite (local DB + WAL/SHM; any common extension)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDE
.idea
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# Local tooling / scratch
tmp
temp
*.orig

# Supabase CLI local state (if you use `supabase start` etc.)
.supabase
